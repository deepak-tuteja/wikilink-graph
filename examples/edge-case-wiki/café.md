# Café

A page whose slug is a non-ASCII, accented word — exercises slug/URL handling (hash routing,
static file fetch, "open in editor" link) beyond plain ASCII filenames.

Back to [[hello]]. See also [[日本語ページ]].
